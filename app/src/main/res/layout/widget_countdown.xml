<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
  android:id="@+id/countdown_root"
  android:layout_width="match_parent"
  android:layout_height="match_parent"
  android:background="@drawable/countdown_widget_background"
  android:gravity="center"
  android:orientation="vertical"
  android:padding="10dp">

  <TextView
    android:id="@+id/countdown_status"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:ellipsize="end"
    android:gravity="center"
    android:maxLines="1"
    android:text="@string/countdown_ready"
    android:textColor="@color/countdown_secondary"
    android:textSize="12sp"
    android:textStyle="bold" />

  <FrameLayout
    android:layout_width="match_parent"
    android:layout_height="0dp"
    android:layout_marginTop="4dp"
    android:layout_weight="1"
    android:background="@drawable/countdown_display_background"
    android:padding="6dp">

    <Chronometer
      android:id="@+id/countdown_chronometer"
      android:layout_width="match_parent"
      android:layout_height="match_parent"
      android:gravity="center"
      android:includeFontPadding="false"
      android:textColor="@color/countdown_dark"
      android:textSize="46sp"
      android:textStyle="bold"
      android:visibility="gone" />

    <TextView
      android:id="@+id/countdown_static_time"
      android:layout_width="match_parent"
      android:layout_height="match_parent"
      android:gravity="center"
      android:includeFontPadding="false"
      android:text="5:00"
      android:textColor="@color/countdown_dark"
      android:textSize="46sp"
      android:textStyle="bold" />
  </FrameLayout>

  <LinearLayout
    android:layout_width="match_parent"
    android:layout_height="38dp"
    android:layout_marginTop="8dp"
    android:baselineAligned="false"
    android:gravity="center"
    android:orientation="horizontal">

    <Button
      android:id="@+id/countdown_subtract_minute"
      android:layout_width="0dp"
      android:layout_height="match_parent"
      android:layout_marginEnd="4dp"
      android:layout_weight="1"
      android:background="@drawable/countdown_control_background"
      android:text="-1m"
      android:textColor="@color/countdown_text"
      android:textSize="12sp"
      android:textStyle="bold" />

    <Button
      android:id="@+id/countdown_add_minute"
      android:layout_width="0dp"
      android:layout_height="match_parent"
      android:layout_marginEnd="4dp"
      android:layout_weight="1"
      android:background="@drawable/countdown_control_background"
      android:text="+1m"
      android:textColor="@color/countdown_text"
      android:textSize="12sp"
      android:textStyle="bold" />

    <Button
      android:id="@+id/countdown_subtract_ten_seconds"
      android:layout_width="0dp"
      android:layout_height="match_parent"
      android:layout_marginEnd="4dp"
      android:layout_weight="1"
      android:background="@drawable/countdown_control_background"
      android:text="-10s"
      android:textColor="@color/countdown_text"
      android:textSize="12sp"
      android:textStyle="bold" />

    <Button
      android:id="@+id/countdown_add_ten_seconds"
      android:layout_width="0dp"
      android:layout_height="match_parent"
      android:layout_weight="1"
      android:background="@drawable/countdown_control_background"
      android:text="+10s"
      android:textColor="@color/countdown_text"
      android:textSize="12sp"
      android:textStyle="bold" />
  </LinearLayout>

  <LinearLayout
    android:layout_width="match_parent"
    android:layout_height="42dp"
    android:layout_marginTop="6dp"
    android:baselineAligned="false"
    android:orientation="horizontal">

    <Button
      android:id="@+id/countdown_start_pause"
      android:layout_width="0dp"
      android:layout_height="match_parent"
      android:layout_marginEnd="6dp"
      android:layout_weight="1.4"
      android:background="@drawable/countdown_start_background"
      android:text="@string/countdown_start"
      android:textColor="@color/countdown_dark"
      android:textSize="14sp"
      android:textStyle="bold" />

    <Button
      android:id="@+id/countdown_reset"
      android:layout_width="0dp"
      android:layout_height="match_parent"
      android:layout_weight="1"
      android:background="@drawable/countdown_control_background"
      android:text="@string/countdown_reset"
      android:textColor="@color/countdown_text"
      android:textSize="14sp"
      android:textStyle="bold" />
  </LinearLayout>
</LinearLayout>
