# Optional: copy this file to .env for repeated wireless installs.
# This file stores connection routing only. Enter the one-time pairing code directly into `adb pair`.
# Never commit real addresses, ports, pairing codes, serial numbers, or MAC addresses.

COZYLA_DEVICE_IP=<device-ip>
COZYLA_ADB_TARGET=<device-ip>:<debug-port>
